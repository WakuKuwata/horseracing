<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan:
`specs/049-harville-stage-discount/plan.md` (active feature: Harville stage 割引 — top2/top3(連対・複勝)確率の校正改善。lgbm-042 は win ECE 0.00057 に対し top2 ECE 0.00735(13倍)・top3 ECE 0.01944(34倍)で、既存 reliability bins(021 で全ラベル永続化済み)により top3 高帯(0.8-0.9)が予測 0.842→実現 0.746=+9.6pt 過大・低帯過小の単調パターン=Harville 既知バイアス(Henery/Stern/Benter)と確定済み(診断フェーズ不要)。手法: 2・3着ステージの逐次分母に Benter 型冪割引 p^λ_j(λ_1=1 固定=win バイト不変・λ=1 明示分岐でバイト一致保証)、λ_2/λ_3∈[0.1,5.0] を条件付き NLL golden-section で独立フィット(min_races=300 未満/境界張り付き=identity fallback)。評価フィット=前 fold pooled OOS 予測(fold 2008=identity)、製品フィット=046 同型 walk-forward 厳密前。実装配置: 依存方向(probability→eval)のため導出コア+フィットは eval/stage_discount.py 新設、baselines.harville_topk に λ 引数、engine.joint_probabilities に stage_discount opt-in(joint marginal==同一λ harville_topk 整合を consistency で検証)。事前登録ゲート: PRIMARY=top2/top3 LogLoss・ECE 改善+strict majority、MUST=win 完全一致+009 不変量+exotic(複勝/ワイド/三連複)pseudo-ROI 差≥−0.005、ガード=worst-fold top3 dLogLoss≤+5e-3。スキーマ・API 変更なし、λ は logic_version(sdisc=...)記録。codex CLI 3 回起動不可→single-opinion(文献+事前登録+research.md D1-D8 の却下代替案で緩和)。048(two_gamma=エンジン入力前 p 校正)とは層が異なり独立合成).

[完了済み feature の要約は以下]
`specs/042-pl-topk-objective/spec.md` (完了済み: Plackett-Luce top-k(listwise)目的関数 — 039 cond_logit(PL top-1)を top-3 に拡張、**lgbm-042 採用済み(機械ゲート通過、036 以来最大の LogLoss ゲイン)**。stage j=1..3 は「未 placed 馬」の softmax で −log p(j着馬)、stage 減衰 w=(1,.5,.25) 固定(spike 事前選択・OOS 調整禁止)。grad/hess は stage 加重加算(hess=max(Σ w_j p(1−p), floor))、stage1 非一意=group 中立化・stage j 非一意 or remaining<2=break。rank ラベル(finish_rank=finished 公式着順 1..3/他 0)は dataset の label 側(win と同一境界・特徴に非流入・結果未確定レースは全 0→中立化)。**予測/校正/serving は cond_logit と同一経路**(SOFTMAX_OBJECTIVES 集合分岐のみ、経路を増やさない=039 最大リスク回避)。**結果(18-fold OOS, baseline=cond_logit+TE+isotonic=lgbm-041 相当)**: pl_topk+isotonic が win LogLoss 0.21792→**0.21706**(−0.00085)・AUC 0.79091→**0.79340**・**ECE 0.00093→0.00058(改善)**・13/18 fold・全ガード内で **ADOPTED 機械通過**。校正 A/B=none は ECE 4.6倍悪化→isotonic。codex の校正懸念は非発現(PL 尤度は確率モデルそのもの)。**codex 昇格ゲート(top2/top3 non-regression)も改善で通過**: top2 0.34352→0.34156・top3 0.43533→0.43220(2-3着教師が Harville 派生を直接改善)。lgbm-042 active・lgbm-041 retired・serving objective=pl_topk ロード確認。FEATURE_VERSION 不変(features-012)・スキーマ変更なし。CLI: `--objective pl_topk`。次=均等 w 純 PL diagnostic 済み後に区切り、または k>3/margin-aware は deferred).

[完了済み feature の要約は以下]
`specs/048-asymmetric-calibration/spec.md` (完了済み: 非対称 p 校正 two_gamma — 047 で判明した「モデル p の非対称 tail 圧縮(本命側乖離>穴側)」に対し、一様 power(017/046)では取り切れない非対称を **p のみを入力とする連続区分 power(two_gamma)** で校正、**事前登録ゲート機械通過で ADOPTED・製品 p 校正を power→two_gamma に切替**。q 条件付き校正は p×q blend=不採用済み方針でスコープ外(p≠q 境界)。w(p)=p^γlo (p≤pivot)/pivot^(γlo−γhi)·p^γhi (p>pivot)=pivot で連続・γ>0 で単調、**pivot=0.15 事前固定**(047 q_band 境界、フィットしない)、(γlo,γhi)∈[0.1,5]² を train 内 winner-NLL で粗グリッド 9×9→座標 golden 2 巡(決定論)。`model_calibration.py` に _apply_two_gamma/fit_two_gamma/fit_p_calibrator(method="two_gamma")、identity fallback 共通(min_races=50/min_wins=30)、**power/identity の lv はバイト不変**(046 後方互換テスト)。**手続き教訓: 事前登録窓「全永続化期間・train_frac=0.5」は予測密度の偏り(2024-11/12 に 551 レース・2025 に孤立 3)で eval n=3=無効(underpowered)** → サンプル数(メタデータ)のみに基づき密集窓 2024-11-02..2024-12-28 へ改訂(結果を見た調整ではない)・ゲート/split 不変で再実行=**今後の校正 A/B は実行前に密度確認**。**結果(train 275/eval 276)**: NLL raw 2.2479→power 2.2194→**two_gamma 2.1954**・Brier 0.8543→0.8538→**0.8476**・ECE 0.0127→0.0076→**0.0060**、**MUST joint(009 後)も exacta/trifecta 両通過 — power では trifecta joint が悪化(6.9835>raw 6.9637)していたのを two_gamma が解消(6.9510)**。フィット γlo≈1.6-1.7(穴側シャープ化)・γhi≈0.4-0.6(本命側押し上げ)=047 所見と正確に整合。製品切替=betting `_fit_product_p_calibrator` method="two_gamma"(serve/backfill 両経路、046 テスト不変)、実 DB E2E(202505040401): 550 informative races でフィット・`pcal=two_gamma;…;pivot=0.15;…` lv 記録・win 9+exotic 30 生成。診断 CLI kelly-calibration-compare の明示 power は据え置き。スキーマ・API 変更無し。codex 見送り宣言(セッション内 2 回起動失敗)。probability 94/betting 138 緑。CLI: `calibrate-eval --method two_gamma`。deferred: q 条件付き校正(方針却下)・3 区分以上/スプライン/isotonic・オンライン再フィット・pivot 再検討(データ後変更禁止=独立 feature で再事前登録)).

[完了済み feature の要約は以下]
`specs/047-segment-diagnostics/spec.md` (完了済み: セグメント別モデル-市場診断 — 「どの条件でモデルが市場に勝てる/負けるか」のセグメント分解が無く運用判断・次レバー発見の材料が無かったのを、**事前登録セグメント定義**(結果を見る前に固定・変更禁止=憲法 III)の診断 CLI で解消。**SECONDARY**(020 規律継承: 採否ゲート・閾値調整に使わない・買いシグナルではない)。スキーマ・API・front 変更無し。codex 見送り宣言(セッション内 2 回起動失敗)。`eval/segment_edge.py`(新): market_edge と同一の walk-forward OOS 収集(fold 毎再学習・p/q/win per horse)+ per-sample identity を保持し、**レース静的属性(races)・厳密前 entries(debut 判定=最古 started 日)・市場 q のみ**からセグメント割当(結果は評価ラベルのみ=属性リーク境界テストで機械固定)。軸=surface/dist_band(020 と同一 bins ≤1400/1800/2200)/q_band(0.05/0.15/0.30)/race_class(新馬/未勝利/OP系/条件)/field_size(≤8/9-13/≥14)/debut。指標=n/勝率/LL(p)/LL(q)/gap=LLp−LLq(正=市場優位)/mean p/q。predictor-agnostic(eval は training 非依存)。CLI `training segment-diagnostic --from/--to`。テスト: バンド境界値・**Σn=全体 reconciliation**・決定論・**結果変更で割当不変**。**実データ所見(2021+ OOS n=181,341、[[segment-diagnostics-047-findings]])**: ①**全セグメントで市場 q 優位**(gap>0 全行=モデルが勝つ条件は無い) ②**gap 最小=中穴帯 q0.05–0.15 の +0.0125**(モデルの相対的土俵) ③**gap 最大=本命帯 q≥0.30 の +0.231** — 市場 0.405≈実現 0.413 に対しモデル p 0.185=**モデル p の tail 圧縮**(穴側は 0.055 vs 実現 0.016=3.5 倍過大)→ **046 の γ>1 シャープ化(実フィット γ=1.34)の方向の独立傍証**、ただし本命側乖離>穴側の非対称は一様 power で取り切れない ④仮説「市場は新馬・少頭数に弱い」**棄却**(debut +0.044>non-debut +0.036、small +0.060>large +0.034)=026 の血統特徴の効き(絶対品質)とセグメント優位(対市場)は別物。eval 51/training 57 緑。deferred: front/API 表示・非対称/条件別校正・診断の定期実行).

[完了済み feature の要約は以下]
`specs/046-calibrated-recommendation/spec.md` (完了済み: 検証済み p 校正の推奨経路組込 — 017(モデル p 校正、eval 両ゲート通過・[p-calibration-017-enabled] で有効化推奨・γ は MLE 任せ)が製品の推奨経路(recommend-serve/backfill→ops/front)に未結線で CLI opt-in 止まりだったのを、**既定組込**した。新校正ロジック無し=017 部品(load_p_samples/fit_p_calibrator[min_races=50/min_wins=30 未満→identity fallback]/apply_p_calibrator)の結線のみ。スキーマ・API・openapi 変更無し。codex CLI 3 回目は見送り宣言(2 回起動失敗)→ single-opinion。(007 win 経路) `generate_recommendations(p_calibrator=)` opt-in — `_apply_p_calibration` が started 再正規化後の p ベクトルに race-normalized 校正(017 canonical、marginal 単体に γ を掛けない)を適用し select_ev_bets/Kelly sizing へ。None=従来バイト同等・lv に pcal 追記(kelly_recommend と同一規律)。(製品フィット) `_fit_product_p_calibrator` = 対象レースより**厳密前**(split_before、race_id タイブレーク)の永続化済み予測×勝者から γ を winner-NLL MLE フィット。スキャンは予測が存在する期間(min race_date of prediction_runs)に bound。(結線) serve=レース前フィット・backfill=**日単位1回フィット**(target_race_id=""で日厳密前=同日除外)で `_generate_product_set` 経由 win/exotic 両群に同一校正器。**実 DB E2E**: 永続化済み予測は 517 レース分あり walk-forward フィットが実成立 — `gamma=1.34303(シャープ化)・n_races=517` は **017 検証時の「2024Q4 γ=1.30 が正解」と整合**、win 12+exotic 30 生成・lv に完全記録(V)。テスト: 007 校正適用で pseudo_roi 変化・None 後方互換・**リーク境界(cutoff=対象レースで train 除外→identity)**・serve の pcal lv 記録。betting 138/api 70 緑。deferred: 013 q 側 FL 補正の製品組込/haircut 既定有効化/γ 条件別・オンライン/無効化フラグ).

[完了済み feature の要約は以下]
`specs/045-win-place-recommendation/spec.md` (完了済み: 単勝(win)推奨の製品結線 — 実査で「複勝(place)は EXOTIC 群として 043 で結線済み(238行)、真の欠落は win のみ 0 行」と判明しスコープを win に絞った。win は real 単勝オッズ(race_horses.odds)を使う **is_estimated_odds=False の唯一の券種**=推定オッズ(double-pseudo)の exotic より EV の信頼度が高い最実用券種。3 点欠落(生成: recommend-serve が 007 を呼ばない/読み出し: API の EXOTIC フィルタ+dict selection 非対応/表示)を結線。新 EV ロジック無し・スキーマ変更無し・migration 無し・**openapi 契約不変**(drift-check 一致)・read-only 014 不変・front 変更ゼロ(win ラベル・real/pseudo 分岐は既存機構)。codex CLI 2 回試行も利用不可 → single-opinion。(読み出し・先行=043 教訓) API クエリを BetType.ALL に拡張し、win の dict selection `{horse_id,horse_number}` を **読み時 [馬番] 正規化**(`_selection_numbers`)で既存 list[int] 契約に載せる(馬番欠損行は除外・500 にしない)。旧「win 除外」テストは新契約に更新。(win Kelly) 007 `generate_recommendations(cfg: KellyConfig|None=None)` — **016 の single_kelly/allocate_kelly 純関数を opt-in 再利用**(win 群は相互排他=単一 allocation 群・real odds=lambda_real・kelly cfg を logic_version に記録)。cfg=None は従来 flat(NULL stake)=後方互換。(群単位冪等) recommend-serve/backfill の冪等を **win 群/exotic 群に細分化**(`_has_group`+`_generate_product_set`)— 043 時代の exotic-only run に win だけ追補・重複なし・backfill に topped_up 集計。**実 DB 検証**: 202504040301(exotic 済み)→ win=10 追補・exotic 非重複、backfill 再実行で 2024-12-28 の 24 レース全 topped_up、API が win 行を selection=[馬番]・real オッズ・Kelly stake 付き・単一 run で返却。テスト: betting 135/api 70/front 69 緑・drift-check 一致。deferred: win 的中/回収バックテスト表示・複勝 real オッズ(netkeiba)・selection 書式 DB 統一).

[完了済み feature の要約は以下]
`specs/044-product-population/spec.md` (完了済み: 製品をデータで満たす・予測 backfill — 021/040/043 で表示・生成の仕組みは完成したが予測(prediction_runs)/推奨が数レース分しか無く大半のレースが空だった製品ギャップを、予測の日付範囲 backfill を追加して解消。新予測/確率ロジック無し(run_serving 006 流用)・スキーマ変更無し(migration なし)・read-only 014 不変・netkeiba 不要・**front 変更なし**(表示は 021/040/043 の既存コンポーネントがそのまま実データ表示)。codex CLI 利用不可のため single-opinion(019 p-parity・025/026 static dtype・043 backfill の既存根拠に基づく)。(US1) `serving/pipeline.py`: run_serving の per-race ループ本体を `_predict_persist(session, model, rid, feature_rows, logic_version)` に抽出(run_serving 挙動不変)+ `run_serving_backfill(date_from, date_to, model_version=None, force=False)` = **日ごとに `build_feature_matrix(end_date=day)`= run_serving と同一経路で予測**(p-parity バイト一致、range 一括ビルドは 026 の static 列 pool 依存 dtype 懸念で回避)・**冪等=`_has_run_for_model`(対象 model_version の prediction_run が無いレースのみ生成)**(force で再生成 append-only)・per-day try/except 例外隔離・`BackfillCounts`(generated/skip_exists/skip_no_started/error_days)。CLI `serving predict-backfill --from --to [--model-version] [--force]`。**冪等ポリシー根拠**: read API(014 select_prediction_run=active→最新)は active モデルの run が無いレースに古い run(例 lgbm-039)を出しうる → 「active モデル run 無し=生成」で gap 補完 + 旧モデルレースの現 active(lgbm-042)更新を両立。(US2) 実 DB: 2024-12-28 に predict-backfill=24 レース(lgbm-042)生成 → recommend-backfill(043)=24 → API が lgbm-042 予測 16 頭+スコア寄与+買い目 30(単一 run・stake)を返すことを確認。憲法 II(run_serving 流用=新リーク面ゼロ・結果非参照・as-of のみ)/III(p-parity・冪等テスト)/V(append-only・logic_version)/VI(migration 無し・read-only 014 不変・CLI のみ)。テスト: serving 27(+3 backfill: p-parity/冪等/force/out-of-range)・betting 133・api 68 緑、run_serving リファクタ回帰なし。deferred: ops job/front からの範囲 backfill 起動・自動スケジュール(019)・予測+推奨を束ねる 1 コマンド・range 一括 build 高速化(dtype 安全性検証後)).

[完了済み feature の要約は以下]
`specs/041-corner-trajectory/spec.md` (完了済み: コーナー通過順の軌跡特徴 — 過去走の「位置の変化」を as-of 集約した新特徴 4 列(asof_late_gain_avg/best・asof_early_pos_avg・asof_mid_move_avg)、FEATURE_VERSION features-011→012・スキーマ変更なし・DB 既存データのみ、**lgbm-041 採用済み(機械ゲート通過)**。late_gain=(最終コーナー通過順−確定着順)/field_size(直線の伸び)・early_pos=初角/field_size・mid_move=コーナー間順位改善max/field_size(捲り)。023 は位置の水準のみでデルタは未活用だった。runs 構築+expanding 集約(全過去走 avg+cummax)+merge_asof(backward, allow_exact_matches=False)=strictly-before+同日除外(023 機構流用)。新ソース列なし=source_fingerprint 無改修・materialize parity bit 一致(917,554×97)・カバレッジ 89%。**採用ゲート(18-fold feature-eval)=ADOPTED 機械通過(026 以来の素直クリア)**: LogLoss 0.23191→0.23185・AUC +0.00025・**ECE 0.00883→0.00858 改善**・13/18 fold・全ガード内。**production 構成(lgbm-041=cond_logit+TE+isotonic+features-012)**: win LogLoss 0.21794(039比−0.000006)・ECE 0.00093・Brier/top2/top3 微改善=強いモデル上の限界寄与は僅少だが全指標非悪化。lgbm-041 active・lgbm-039 retired・serving 95特徴ロード確認。040 leak-guard の禁止トークン gain→split_gain に絞り(late_gain はドメイン用語、importance/contribution で 040 リーク面は担保)。**2026-07-02 ブレストキュー決着**: ensemble=校正コストで不採用/rank-gap=flat 不採用/corner 軌跡=本 feature 採用/**PL top-k(top-3, w=1/.5/.25)=winner-NLL −0.0095・top1 +0.0064・AUC +0.0035 の強信号 → 次 feature 042 で実装予定**(039 infra 流用、校正 A/B と win LogLoss 非悪化ゲートは 039 同型で)。recency/blend は不採用済).

[完了済み feature の要約は以下]
`specs/043-recommendation-generation/spec.md` (完了済み: 製品を実データで通す・買い目(推奨)生成 — read-only 製品(021/040)は表示の器が完成したが `recommendations` テーブルが 0 行で買い目/EV/Kelly が画面に出ない製品ギャップを、既存 011/016 生成ロジックを製品フローに結線して解消。新予測/EV/Kelly ロジック無し・スキーマ変更無し(migration なし)・read-only 014 不変・netkeiba 不要。**codex second-opinion 反映(読み出し是正が先)**: 素朴に生成ボタンだけ足すと壊れて出荷される(実査で判明: ①`api.queries.exotic_recommendations` が prediction_run で絞らず append-only 重複がそのまま表示 ②`RecommendationRow` に stake_fraction 未露出=Kelly 出ない ③exotic と Kelly が別行=両方呼ぶと二重表示)。(US1) 読み出し是正: `exotic_recommendations(race_id, *, prediction_run_id)` を select_prediction_run と同一 run で絞る・`RecommendationRow` に recommendation_id/stake_fraction 追加・front RecommendationPanel に Kelly比率列+行 key=id(pseudo バッジ維持)。生成 = `betting recommend-serve --race-id`(API 一致の active-model run 解決[betting は api を import 不可のため resolver 複製]・**冪等 skip**[選択 run に既存なら再生成せず append-only 重複を作らない]・**単一セット=`generate_kelly_recommendations` のみ**[EV 選定+Kelly stake、exotic 二重生成しない]・run無/odds無→SKIPPED)。(US2) ops `recommend` job = betting CLI を subprocess(`uv run --project betting`、028 predict 同型 cwd/VIRTUAL_ENV strip/timeout/exit-code→succeeded|skipped|failed)で ops の ML/betting 非 import 境界維持・`POST /ops/v1/races/{id}/recommend`→202・front RecommendButton(予測ボタンと別)・ops-openapi/型再生成。(US3) `betting recommend-backfill --from --to`(冪等・per-race 例外隔離・件数 reconciliation)。**実 DB 検証**: recommend-serve→30 recs+冪等 skip、backfill→2024-12-28 の 24 レース一括(24=24)、API 単一 run・stake 露出。憲法 II(ops subprocess 境界・推奨値は特徴に戻さない・結果を選定に読まない)/V(冪等+append-only+logic_version)/VI(schema/openapi 先行・migration 無し)。全パッケージ緑(db 53/betting 133/api 68/ops 44 境界維持/front 69)+drift-check 緑。deferred: 予測完了時の自動生成/単複(007)結線/batch id 列/real exotic odds(netkeiba 依存)/スケジュール実行).

[完了済み feature の要約は以下]
`specs/040-prediction-explanation/plan.md` (完了済み: 予測根拠表示 — Obsidian vault 表示要件の最後の未実装(open-decisions P2)。表示専用 3 点: (US1) serving 予測時に LightGBM `pred_contrib`(TreeSHAP 内蔵・新依存なし)で **booster margin(raw_score=True、cond_logit の race-softmax/isotonic 校正の前)** への特徴寄与を分解、top-K(5)+監査付帯(method/method_version/base_value/score/other_contribution、保存単体で加法性 INV-E1 検証可)を `race_predictions.explanation`(新 nullable JSONB、**migration 0008**=012/016 以来のスキーマ変更、憲法 VI 正当化済) に永続化 → API SELECT 透過 → front 馬行展開「スコア寄与」バー(日本語 featureLabels.ts 単一対応表・te_* は「導出特徴」バッジ・限界注記常時=「校正・レース内正規化前のスコア寄与」「因果ではない」)。(US2) gain 重要度を metrics_summary["importance"] 追記(スキーマ変更なし)→ `GET /models/{mv}/importance`(021 calibration 同型、未収録 404)→「分割利得(gain)重要度」限定命名チャート。(US3) p vs q(021 同一 canonical field)乖離を読み時純関数で 3 バンド(事前登録・変更禁止: p ⋛ q±max(0.03,0.5q))→ **純事実比較バッジ**(「市場評価がモデルより高い」等。codex R3: 弱気/強気も市場助言的で却下。危険/妙味/edge 語・損益色・乖離ソート禁止=021 継承)、q 欠損/canonical_consistent=false で抑制、odds_as_of 時点参照。**リスク先頭 T0 spike**: 実 lgbm-039 booster で寄与再構成検証(Σcontrib+base==raw_score margin、serving と同一 X=TE 適用後 feature_cols 順)+レイテンシ実測、不成立なら中断(039 教訓)。予測 p バイト一致(INV-E2)・リーク境界(explanation/importance/divergence はモデル特徴に戻さない leak-guard)・API read-only/ML 非依存維持・feature_snapshots 契約不変(explanation を混入しない=codex 却下案)。旧 run は NULL=未提供。codex second opinion 反映済(監査付帯保存・純事実文言・再構成レシピ・導出特徴バッジ・as_of 明示)。deferred: SHAP interaction/反実仮想/自然文生成/危険度スコア数値化/AdminFront。**実装完了**: T0 spike GO(実 lgbm-039 で加法性 max_rel 1.1e-14≪1e-6・latency 3-6ms/レース)→ `training/explanation.py`(compute_explanations, 防御的 try/except で予測経路を絶対壊さない)→ serving predict_race/persist_run 結線(explanations 第3戻り値)→ migration 0008(race_predictions.explanation JSONB)→ api schemas/queries/router 透過 + `/models/{mv}/importance`(importance.py, 021 同型) + selection.divergence_band → front featureLabels.ts/ExplanationPanel/DivergenceBadge/ImportanceChart + HorseEntriesTable 行展開結線 + openapi 再生成/drift-check。**実 DB e2e 検証済**(202504040301: 15/15 explanation 保存・加法性 diff 0.0・top=jockey_place_rate・API 透過・divergence=model_higher)。全11パッケージ緑(features 111/api 66/serving 23/betting 130/live 17 等、predict_race 3-tuple 化の呼び出し元も修正)。**importance は lgbm-039(040 以前学習)で 404 未収録=設計通り**(FR-015 再学習不要、次回学習で populate)。head assert 波及(0007→0008: features/live)修正済).

[完了済み feature の要約は以下]
Conditional-logit 目的関数 (039, lgbm-039 active) — 036 と同型のモデリング変更(新特徴なし・スキーマ変更なし・FEATURE_VERSION 不変 features-011)、**lgbm-039 運用採用済み**。現行 binary は各馬独立 P(win)→009 で後付け正規化だが、実構造は「1レース1勝」= フィールド上の多項。各馬スコアをレース内 softmax `p_i=exp(s_i)/Σexp(s_j)`、損失 −log p_winner を直接最適化する LightGBM custom objective(4.x `params["objective"]=callable`、group=race、grad=p−y・hess=max(p(1−p),1e-6)、sum(y)!=1 の group は grad/hess=0 で学習中立化)を `cond_logit.py` に追加。出力 softmax が自然に Σ=1(009 整合、憲法 IV)。cond_logit は fit/predict/serving 全入口で group(race_id)必須(binary 不要=後方互換 bit 一致)。**結果(18-fold OOS, baseline=binary+TE+isotonic=lgbm-036 相当)**: cond_logit+isotonic が win LogLoss 0.21851→**0.21794**(−0.00057)・AUC 0.78957→**0.79070**・**mean ECE 0.00393→0.00096(4倍改善)**・15/18 fold。**校正 A/B(codex 是正: softmax は既に Σ=1 → per-horse isotonic は heuristic 再配分)= isotonic が none(ECE 0.00178)に勝つ**。事前登録ゲートは worst-fold ECE +0.00307>2e-3 で機械 False(020/023 と同じ単一 fold blip・mean は 4倍改善)→ **ユーザー判断で運用採用(023 前例)**: lgbm-039 active 昇格・lgbm-036 retired。**serving 安全**: feature 列不変=feature_hash=features-011 整合、objective/postprocess を metadata 記録、raw_predict が cond_logit で race-softmax。CLI: `model-eval/train-evaluate --objective cond_logit --calibration {isotonic,none}`。**注意**: predict の意味が objective で変わる(独立確率↔race-softmax)→ eval/calib/serving 全経路で同一 postprocess(group softmax→校正→009)を通す(codex 最大リスク、実装反映済)。次=recency 重み付け・p×q blend(製品確率)・cond_logit×recency 併用、または lap full backfill 再開).

[完了済み feature の要約は以下]
区間ラップ ingest (034, main マージ済) — §4 第1段。db.netkeiba の race ページから 200m 毎ラップ + テン3F/上がり3F を抽出し新テーブル race_laps(migration 0007)に格納。parser/スキーマ/CLI scrape-laps(日付範囲 backfill)。実 fixture unit + testcontainer integration 緑。区間ラップ特徴(035)は branch 保全(features-012 マージは lgbm 破壊)、部分backfillのfocused OOSで正の信号、full backfill後に正式採否。scrape_laps は per-page 例外で job 全体 abort するバグあり(resilience 修正要)。

[完了済み feature の要約は以下]
条件替わり×能力/時計 交互作用 (033, features-010→011, lgbm-033 active) — §3 中コスト第3弾。027 の未マージ条件替わり base(dist_change/surface_switch/going_change=現 main に無い新情報)を新モジュール `condition_change_features.py` に再導入し、距離替わりの符号付き hinge(dist_extension/dist_shortening)× 023 の末脚/時計能力(rel_last3f_best/rel_time_avg)との交互作用 2 列を加える(計 7 列)。027 の未マージ条件替わり base(dist_change/surface_switch/going_change=現 main に無い新情報)を新モジュール `condition_change_features.py` に再導入し、距離替わりの符号付き hinge(dist_extension/dist_shortening)× 023 の末脚/時計能力(rel_last3f_best/rel_time_avg)との交互作用 2 列を加える(計 7 列)。**032 の学び反映**: 「積でなく新情報が主役」→ 主役は 027 の新 base、hinge×能力で「027 単独不発(8/18)を効く形に変換」。class×time/斤量×time の既存列積は GBM 冗長で除外(codex)。base は直前 started レース(merge_asof allow_exact_matches=False)、能力は 023 as-of 出力で今走非参照。025 単一源結線(pace 渡し)・bit パリティ・going は races 既存ロードで source_fingerprint 無改修。FEATURE_VERSION 010→011。**結果(18 fold OOS)=ADOPTED(弱いが通過)**: win LogLoss 0.23193→0.23187(−0.00006)・AUC 0.75153→0.75197(+0.00044, 032 の 4 倍)・ECE 0.00858→0.00878(微悪化 tol 内)・11/18 fold。027 base を hinge×能力に変換したら AUC が持ち上がった=「新情報を効く形に」が奏功。ECE 微悪化は 017 p校正で相殺。lgbm-033 active 昇格・lgbm-032 retired。実 DB parity bit 一致(916k×93)。次=§3 残り交互作用 or §4 sectional(DB 未取得データの ingest 拡張)).

[完了済み feature の要約は以下]
低履歴×血統適性 交互作用 + 種牡馬デビュー戦適性 (032, features-009→010, lgbm-032) — §3 中コスト第2弾。市場が弱いデビュー/低履歴馬に効く血統シグナルを強化。新モジュール `debut_pedigree_features.py` が 5 列: (a) **新情報 `sire_debut_win_rate`** = 同種牡馬の **他産駒のデビュー戦(各馬初出走)勝率**(026 `_other_offspring` 自馬除外機構を debut-runs サブセットに適用、strictly-before・同日除外)、(b) ゲーティング交互作用 4 列 = is_debut/is_low_history × sire_win_rate/sire_dist_band_win_rate の積。**codex 反映**: 「既存特徴同士の単純積は GBM が木分割で学習済み=冗長」→ 新情報(a)を主役・(b)は副次で bundle OOS が採否(030 前例)。031 の勝ち筋(モデルが持たない条件付き集約)を血統に適用し市場弱点(デビュー馬)を直撃。025 単一源結線・bit パリティ・新ソース列なし(sire_name は 026 で fingerprint 包含)で source_fingerprint 無改修。FEATURE_VERSION 009→010、スキーマ変更なし。採用=事前登録 bundle OOS(feature-eval --drop-groups debut_pedigree)、SECONDARY=デビュー馬セグメント診断(全体は出走比10.5%に希釈されうる)。**結果(18 fold OOS)=ADOPTED(僅差)**: win LogLoss 0.23200→0.23193(−0.00007, 031 の 1/10)・AUC 0.75143→0.75153・ECE 0.00878→0.00858(改善)・10/18 fold(strict majority ぎりぎり)。**codex 事前見立て的中**(単純積=GBM 冗長・全体は debut 出走比に希釈・採用 20-35%)→ 僅差で採用側着地、ECE 改善+全ガード通過で機械的クリア(数値を見てから閾値を動かさない=憲法 III)。実質価値はデビュー馬(95,070 頭・sire_debut カバレッジ 89.8%)。lgbm-032 active 昇格・lgbm-031 retired。実 DB parity bit 一致(916k×86)。次=Feature 033 条件替わり×能力/時計(027 の dist_change/surface/going 再導入+hinge×末脚、新 base 列が主役・積は副次と見込む)).

[完了済み feature の要約は以下]
展開・ペース構成特徴 (031, features-008→009, lgbm-031 active) — §3 中コスト第1弾。repo 初の field-composition(出走馬の組合せ=展開)特徴。023 `build_pace_features` が算出済みの各馬 as-of 優勢脚質(front_runner_rate/closer_rate/rel_corner_pos_avg)を **今走 race_id 内で leave-one-out(自馬除外)集約** し、レース展開と「自馬脚質×フィールド構成」の相互作用を作る。新モジュール `pace_scenario_features.py` は build_pace_features の **出力のみ** を入力に取り生今走列を読まない⇒リーク安全が構造的に保証(他馬の **過去 as-of** のみ)。7 列(field_front/closer_rate_ex_self・pace_imbalance_ex_self・front_pressure・closer_setup・style_mismatch・field_style_coverage)、全 float64・NaN 伝播(0埋め禁止)・coverage で Unknown 量明示。フィールド母集団=entry_status==STARTED(取消は serving と一致、result は読まない)。025 build_asof_features 単一源に結線・bit パリティ維持・新ソース列なしで source_fingerprint 無改修。FEATURE_VERSION 008→009、スキーマ変更なし。採用=事前登録 bundle OOS(feature-eval --drop-groups pace_scenario で baseline=features-008 vs candidate=009、020/023/030 同型ゲート)、ablation/market_edge は SECONDARY。codex 反映(leave-one-out 連続量・相互作用主役・0埋め禁止・coverage 列・bundle 事前登録、カテゴリ化=027 同型で却下)。**結果(18 fold OOS)=ADOPTED**: win LogLoss 0.23277→0.23200(−0.00077, 本シリーズ最大級の単群ゲイン)・AUC 0.74810→0.75143(+0.0033)・ECE 0.00893→0.00878(改善)・17/18 fold。**020/023(識別力↑だが ECE 悪化)と違い AUC↑かつ ECE↓ を両立**=展開(組合せ)シグナルが単独能力特徴の届かない新情報を追加 → **採用(lgbm-031 active 昇格・lgbm-030 retired)**。実 DB parity bit 一致(916k×81)。deferred=Q3 の他中コスト交互作用(距離×末脚/クラス×時計/枠×脚質/斤量×能力/低履歴×血統)は §3 後続、furlong sectional/隊列シミュ/脚質埋め込みは §4).
Stack: Python 3.12, PostgreSQL 16, SQLAlchemy 2.0, Alembic, psycopg3, pytest + testcontainers; numpy/scikit-learn/pandas/lightgbm for ML; httpx + selectolax/bs4 for scraping.
Packages: `db/`, `ingest/`, `eval/`, `features/`, `training/`, `serving/`, `betting/`, `scrape/`, `probability/` (`horseracing-probability`).
Probability: from per-race win probs derive all 7 JRA bet-type probabilities via Plackett-Luce/Harville — exacta P(i→j)=p_i·p_j/(1−p_i), trifecta sequential; quinella/trio = sum of orderings; wide{i,j}=Σ_k trio{i,j,k} (NOT independent product); place=harville top-N (field-size: 5–7=top2, 8+=top3, ≤4=none). Order FIXED: exclude scratched → renormalize Σ=1 → clip[eps,1−eps] → derive (renormalize BEFORE PL denominators); do NOT inherit harville's denom-skip. Invariants: Σexacta=1, Σtrifecta=1, unordered=sum-of-orderings, joint marginals == harville_topk, includes∈[0,1], monotone, deterministic. Derivation never reads results/odds (leak boundary); calibration eval vs independent-product baseline (NLL/Brier). No schema change. Exotic odds/EV/estimated-odds deferred (P0).
Estimated market odds (010, in `probability/market_odds.py`): from WIN odds derive market-implied q_i=(1/odds_i)/Σ(1/odds_j) (vote share — NOT true prob, NOT model p, contains favorite-longshot bias) → feed q to the 009 engine → per-bet-type market-implied prob → estimated odds = (1−takeout_b)/P_market. takeout JRA defaults (win/place 20%, quinella/wide 22.5%, exacta/trio 25%, trifecta 27.5%), configurable + in logic_version. win-odds recovery: estimated = R·S·odds_i, exact when R·S=1. PL-extrapolated exotic odds diverge from real exotic pools → flagged 推定/pseudo (is_estimated). Conversion uses market odds ONLY (never model p; p and q kept separate). Invalid/scratched excluded + q renormalized; P→0 caps the DERIVED odds, not the probability. Eval: win-odds recovery + q calibration (NLL/Brier), pseudo. No schema change. Exotic EV/recommendations/persistence deferred.
Exotic EV (011, in `betting/`, depends on `probability`): exotic recommendation = P_model(009 on MODEL win prob p) × O_est(010 on MARKET win odds q); EV=P_model(c)·O_est(c), keep p≠q (never mix). CRITICAL: compute P_model and O_est on ONE canonical field (horses with valid p AND valid odds) — renormalize each engine's input on that shared population, else EV multiplies mismatched populations. selection stored JSONB-safe (ordered array for exacta/trifecta, sorted array for quinella/wide/trio, single for place — NO frozenset). Per-(race,bet_type): EV≥threshold, top-K by (−EV, selection_key), flat stake. recommendations: market_odds_used=null, estimated_market_odds_used=O_est, is_estimated_odds=true, pseudo_odds=1/P_model, pseudo_roi=EV−1, append-only. Pseudo-ROI backtest: per-bet-type hit (exacta/trifecta=ordered, quinella/trio=set, wide/place=inclusion + 009 field rule), place/wide MULTIPLE hits scored bet-level, payout=stake×O_est, vs baselines (lowest-O_est / uniform) same conditions, success=beats baseline (NOT >1.0). DOUBLE-pseudo (estimated odds + PL extrapolation) labeled everywhere. Selection never reads results (leak boundary); results used only for scoring. No schema change. Real exotic odds/Kelly/bias-correction deferred.
Real exotic odds (012, NEW table `exotic_odds` — first NEW table since the foundational schema/contract migrations 0001–0004; features 006–011 added no schema; justified under principle VI): reuse 008 polite netkeiba infra to ingest REAL exotic dividend odds (place/quinella/exacta/wide/trio/trifecta). `exotic_odds` = SINGLE latest value per (race_id, bet_type, selection) + updated_at, NO snapshot history (constitution V, same as race_horses.odds) — codex's odds_phase 2-row proposal REJECTED for violating V; pre-race scrape=morning odds, post-result scrape=final dividend overwrite (netkeiba is sole source, no JRA-VAN to protect). selection stored via 011's to_selection (SAME canonical array; ordered exacta/trifecta, sorted quinella/wide/trio, single place) → UNIQUE(race_id,bet_type,selection) B-tree for exact join. netkeiba IDs via id_mappings only; idempotent ON CONFLICT overwrite; ingestion_jobs job_type='exotic_odds' + summary (expected/observed/missing). betting wiring: real odds present → market_odds_used=real, is_estimated_odds=false, real ROI; else fall back to 011 estimated (double-pseudo) — row-level distinction, matched via canonical_field/to_selection single path; post-recommendation scratch → void/skip. Backtest payout = real dividend when present else O_est, labels separated. Eval-first: estimated(010/011) vs real divergence per bet_type/race (coverage_rate, signed log(real/est) median/MAE/P90, estimated=baseline). Exotic odds NEVER a model feature (leak boundary). coverage_scope full/partial; full grids proven by expected-count tests. Real full-grid guarantee / Kelly / bias-correction deferred.
FL bias correction (013, in `probability/`, extends 010, no schema change): market vote-share q=(1/odds)/Σ(1/odds) contains favorite-longshot bias; calibrate q→q' against realized 1st-place outcomes and feed q' to the 009 engine for corrected estimated odds (010) and EV (011/012, opt-in via `estimate_market_odds(calibrator=...)`; raw-q path kept backward-compatible). CRITICAL (codex): fit/evaluate the RACE-NORMALIZED q'_i=g(q_i)/Σg(q_j), NOT the per-horse marginal g(q_i) (renormalization changes marginals) — canonical method is the power model q'∝q^γ with γ by winner-likelihood MLE; ensure our normalize+clip matches the 009 engine so the passed q' is a no-op (evaluate the exact vector the engine uses); field_size from the corrected running set. Leak boundary: q' is MARKET-derived, kept separate from model p (p≠q); odds/q/q' NEVER a win-model feature (leak-guard test). Train-only/walk-forward STRICTLY before target race start (race_id tie-break, never date-level <=); method/hyperparam selection inside the training window only (no selection leak). Eval-first: PRIMARY = q vs q' win-rate calibration (NLL/Brier/ECE/reliability per fixed popularity band, on normalized q') is the ADOPTION GATE; SECONDARY = 012 estimated-vs-real exotic divergence before/after is DIAGNOSTIC only (real pools have their own takeout/bias). Corrected estimated win odds intentionally do NOT recover raw odds (bias removed). race_horses.odds is closing-leaning → retrospective study; operational uses pre-race odds (limitation disclosed). Dead heats excluded from calibration (count surfaced). Estimated odds still pseudo-labeled. Multi-output model / Kelly / model-p-side bias correction deferred.
Prediction-serving API (014, NEW `api/` package — repo's first web layer: FastAPI + uvicorn + pydantic, no schema change): read-only JSON serving of existing persisted data (races/predictions/odds/recommendations) so the future React/Vite front (015) consumes a stable OpenAPI contract (constitution VI: API/DB contract before any UI). CRITICAL (codex): the API is strictly READ-ONLY — `/recommendations` SELECTs persisted `recommendations` rows ONLY and must NEVER call `generate_exotic_recommendations` (a write); `api/` depends on db (ORM read) + probability (pure 009 joint_probabilities / 010 estimate_market_odds) ONLY, NOT betting (no write-path exposure). prediction_run selection is deterministic (adoption_status='active' → computed_at DESC → prediction_run_id tie-break) and the chosen run_id is returned. Joint probabilities only on `?bet_type=&top=K` (never a full trifecta grid unprompted); computed on the canonical field (scratched excluded+renormalized). Odds endpoint keeps real vs estimated in SEPARATE fields (win real / 010 estimated pseudo / 012 real_exotic) with odds_source/is_estimated/coverage_scope/updated_at — never conflated; recommendations expose is_estimated_odds/pseudo_odds/pseudo_roi/double_pseudo so the front can't read pseudo-ROI as realized. Missing → typed 404 / 200-empty / 409-422 (never an unhandled 500 from pure-helper exceptions). Endpoints under `/api/v1/`; OpenAPI/`/docs` auto-generated. ASGI lifespan app-scoped engine/sessionmaker + per-request read-only session (rollback/close, no commit). Response values NEVER re-enter model features (leak boundary). Auth / writes / Kelly / deploy / the React front deferred.
RaceFront (015, NEW `front/` package — repo's first JS/Node layer: React + Vite + TypeScript, read-only SPA): consumes the 014 read-only API (`/api/v1`, OpenAPI) to show a race list + race detail (predictions/odds/recommendations). Does NOT touch the API or DB (014 stays unchanged, CORS-free; the SPA calls relative `/api/v1/*` via a Vite dev proxy → http://localhost:8000; prod reverse-proxy/CORS deferred). CRITICAL (codex, constitution V): pseudo values (estimated odds / pseudo_odds / pseudo_roi / double_pseudo) must NEVER render without a badge — enforced via TS discriminated unions on odds_source/is_estimated/double_pseudo, a single `<PseudoBadge>`/`<SourceBadge>` render path, and a named invariant test asserting "no pseudo value without a label" (so a user can't misread a pseudo-ROI as realized). real vs estimated odds shown in SEPARATE sections (never conflated). Audit (prediction_run_id/model_version/logic_version/computed_at, estimated as_of) shown on screen, not buried in tooltips. Loading / 200-typed-empty / typed-error(404/409/422 {status,code,detail}) are three DISTINCT UI states; nullable numbers (win/top2/top3/odds/pseudo_*) rendered safely via a central formatNum (→ `--`/未提供, never NaN). Types AUTO-GENERATED from the API via openapi-typescript with a committed `front/openapi.json` snapshot + committed generated types + a drift-check test (constitution VI: front types must not diverge from the locked 014 contract). Read-only: no write/form UI; nothing flows back to model features (II). Tests = Vitest + React Testing Library + MSW. Auth / writes / Kelly / deploy / Playwright E2E deferred.
Kelly staking (016, in `betting/`, depends on `probability` 009/010 + reuses 011/012 canonical_field/to_selection/recommendations): size HOW MUCH to bet — flat stake → Kelly. Per bet edge=P_model·O−1, f*=edge/(O−1), effective=clip(λ·f*,0,cap_bet); edge≤min_edge or O<o_min → skip. CRITICAL (codex): same-(race,bet_type) bets are MUTUALLY EXCLUSIVE (1 winner) so allocation maximizes joint expected-log-growth G(f)=ΣP_c·log(1−S+O_c·f_c)+(1−ΣP_c)·log(1−S) (concave, deterministic projected-gradient) NOT independent f* summed (heuristic kept; its over/under-bet vs exact is measured in backtest, FR-004). p≠q held: probability is ALWAYS P_model (009 on model p), odds are real exotic (012) preferred else estimated O_est (010, double-pseudo); q never used as a probability; stake_fraction/odds never re-enter model features (leak-guard test, II). Estimated-odds Kelly is誤差-sensitive (denominator O−1) → conservative λ_est<λ_real + min_edge_est + o_min + caps + enable_estimated toggle; labeled double_pseudo (≡is_estimated_odds, API-derived). Schema: ONE nullable column `recommendations.stake_fraction` (migration 0006, justified VI); flat 011/012 rows stay NULL; λ/cap/bankroll/alloc/odds-source encoded in logic_version so stake=fraction×bankroll reproduces (V). Eval-first bankroll backtest: walk-forward sequential bankroll, Kelly vs flat same conditions; metrics terminal-bankroll/log-growth/maxDD/ruin-probability/variance/streak; ruin via SEEDED block bootstrap (preserves serial order; NO i.i.d. shuffle; seed recorded) not analytic; real vs double_pseudo segments scored SEPARATELY; success=risk-adjusted growth beats flat (NOT bare ROI>1). Selection never reads results (採点 only). Multi-bet-type simultaneous Kelly / cross-type correlation / real-money ops / model-overconfidence (edge haircut) deferred.
Model-p calibration + edge haircut (017, in `probability/` model_calibration + `betting/` calibration_eval; depends 009/016, mirrors 013): Kelly amplifies probability error (f*=edge/(O−1)), so calibrate MODEL p before Kelly + haircut the edge. p side mirror of 013's market-q side, kept separate (p≠q): the p calibrator is NEVER applied to the market-odds path. Canonical method = power/temperature p'∝p^γ (γ=1/T; γ<1 softens overconfidence), γ by race-normalized conditional-logit winner-NLL golden-section MLE (reuses 013's _golden_min on p); p' run through the 009 engine normalize+clip so evaluated==engine vector. CRITICAL (codex): marginal p calibration does NOT guarantee joint (PL non-linear) → 009-後 per-bet-type reliability non-degradation is a MUST adoption gate; method/hyperparam selection stays inside each walk-forward fold (race_before strictly-before, no date-level <=), small window → identity fallback (035/036 selection-leak precedent). edge haircut role-separated (calibration=systematic overconfidence, haircut=residual/model risk), relative (edge·(1−h)) default, independent on/off; reported edge (pseudo_roi) stays RAW, only sizing uses adjusted edge. Eval-first adoption gate = NLL/Brier (主) + ECE/reliability (補助) + overconfidence metrics (reliability slope / top-band over-under / calibration-in-the-large) + **009-後 joint non-degradation + Kelly risk non-degradation as MUST gates** (校正 improved but Kelly worse の逆転は不採用; ROI>1 単独不可). 2×2 (raw/cal p × raw/cal q) detects double-correction edge over-shrink; order fixed q→O_est→p. Dead heats excluded from fitting (count surfaced). p'/haircut/edge_adj/Kelly fraction NEVER re-enter model features (leak-guard). NO schema change (calibration in logic_version; opt-in to kelly_recommend/kelly_backtest/009, raw p backward-compatible). Multi-output/joint direct calibration / online / per-condition calibration / uncertainty-aware Kelly deferred.
Production deploy (018, NEW `deploy/` — repo's first infra layer; no schema change): containerize 014(API)+015(front) and serve them single-origin so 015's deferred prod reverse-proxy/CORS is resolved. nginx serves the static Vite build AND reverse-proxies `/api/v1/*`/`/openapi.json`/`/docs` to uvicorn on the SAME origin (no CORS); `location ^~ /api/` precedes the SPA `try_files` fallback and PRESERVES the path (no trailing slash on proxy_pass) so unknown `/api/*` returns the API 404, never index.html; history fallback only under `/`. docker compose: db(postgres:16, pg_isready) → migrate(one-shot, owner role, `alembic upgrade head` + idempotent read-only role via deploy/scripts/migrate.py) → api(app_ro, healthcheck) → nginx(:8080). CRITICAL (codex): FAIL-CLOSED — api `depends_on: migrate condition: service_completed_successfully` so a failed migration blocks serving; `/api/v1/health` is read-only-extended to verify DB + alembic_current==bundled head (503 if not), preventing silent un-migrated start. DB PRIVILEGE separation: serving role `app_ro`=SELECT-only (read-only enforced by DB, not app), migration uses owner; ALTER DEFAULT PRIVILEGES covers future tables, write REVOKEd. API image = multi-stage uv, build context=repo ROOT, dependency closure {api,db,probability,eval} (probability→eval), non-root, `uv sync --frozen`; front image = node build + nginx, `pnpm install --frozen-lockfile`. Reproducibility: pinned base tags (digest for release), git SHA label; live `/openapi.json` == committed `front/openapi.json` (015 type sync) verified. Secrets via env only (`.env` non-committed, `.dockerignore`); compose postgres is local/staging, prod uses external managed DB via DATABASE_URL. read-only invariant kept (no new write endpoint; migration is the explicit startup write exception). Verified end-to-end with real Docker (smoke/routing/readonly/fail-closed scripts). k8s/Helm / TLS automation / CI-CD / secret manager / observability / DB backup automation / live serving deferred.
Live serving (019, NEW `live/` package — orchestration layer; no schema change): produce predictions+recommendations for UPCOMING (result-pending) races using pre-race odds, resolving the closing-leaning limitation disclosed in 010–017. THIN wiring: guard → (optional) scrape(008) → run_serving(006) → recommend(011/016) → prospective report. KEY: `run_serving` already builds as-of leak-safe features (`build_feature_matrix(end_date=race_date)`, no results read, same-day excluded, result-pending-safe) — 019 reuses it and adds NO new prediction logic (no new leak surface). CRITICAL (codex): FAIL-CLOSED guards (guards.py) before any write — valid JRA-VAN 12-digit id, **result-pending = NO race_results rows** (robust "not-yet-run" signal, not wall-clock), entries_complete; odds_present gates ONLY the recommendation step (prediction needs no odds → missing odds skips recs, keeps prediction). recommendations persist the USED odds value (market_odds_used/estimated_market_odds_used) + computed_at, not just as_of (reproducible/audit). cutoff = race_date (post_time column exists but is mostly null; 004 is date-level; time-level cutoff deferred). Eval without results (III): (a) p-PARITY — live path == retrospective run_serving give identical win_prob (leak-free); past pre-race odds are NOT retained (closing overwrote them) so recommendation/EV past-parity is OUT of scope, parity is on p only; (b) leak test — adding race results doesn't change its prediction; (c) prospective log — generated prediction_run/recommendations (computed_at + used odds) feed existing 007/011/016 backtest once results land. live Kelly is SHADOW (recorded, no real stakes). scrape is URL-driven or DB-state-driven (operator passes netkeiba URL or runs 008 first); race_id→netkeiba URL auto-resolution deferred. new `live/` reuses scrape/serving/betting/db (avoids reverse-dependency). NO schema change (reuses prediction_runs/race_predictions/recommendations + 008 tables + 016 stake_fraction). Auto-scheduling / real-money / push / odds-history / multi-source / time-level cutoff deferred.
Model improvement (020, in `features/` + `training/` + `eval/`; no schema change, FEATURE_VERSION bumped features-004→features-005): 016/017 の実データ評価で「モデルは exotic 推定オッズ上で市場に勝てていない」と判明 → 予測 win 確率の識別力/校正の底上げを目的にリーク安全な新特徴を 4 group 追加し、walk-forward OOS で baseline(004 のみ) を上回る時だけ採用。新特徴 9 本/4 group = recent_form(avg_last3_finish/recent_win_rate)・aptitude(dist_band_win_rate/dist_band_avg_finish/surface_win_rate)・race_condition(class_transition/field_size)・human_form(jockey_win_rate/trainer_win_rate)、`registry.FEATURE_GROUPS`+`FEATURE_VERSION`。リーク境界(憲法 II、035/036 校正ミス前例): 全 as-of は対象レースより前のみ＝004 の `_cumulative_before`(daily cumsum−当日)+`merge_asof(allow_exact_matches=False)` 機構を転用、跨馬統計(騎手/調教師)は daily cumsum−当日で対象行+同日を除外(TE 的リーク回避)、odds/結果は特徴にしない(leak-guard test)。採用ゲート(憲法 III、analyze F1): 候補特徴は事前固定(OOS で特徴選択しない＝評価モデル==デプロイモデル)、fold 内はハイパラ/early-stopping のみ、PRIMARY=平均 win LogLoss 改善 かつ ECE 非悪化 + fold 別差(勝ち fold 過半・最悪 fold ECE 非悪化)で偶然 fold 排除、`predictor.drop_features` で baseline=020 全 group drop。`eval/feature_eval.py`(AdoptionReport)・`ablation.py`(group 寄与、diagnostic で採否に使わない)・`market_edge.py`(p−q calibration/edge bucket 実現勝率/q 条件付き LogLoss)は PREDICTOR-AGNOSTIC(eval は training に依存しない＝循環回避、`LightGBMPredictor`+`FEATURE_GROUPS` は `training/cli.py` の feature-eval/feature-ablation/feature-diagnostic が注入)。SECONDARY(diagnostic のみ、主ゲートにしない): pseudo-ROI/Kelly は betting 側、市場 q 超過 edge は market_edge — 「絶対校正改善≠市場超過」を明示し成功基準を OOS win 改善に置く。実データ評価: 2007–2008/1 fold では新特徴は baseline 0.23880 を超えず adopted=False だったが、データ拡張(2007–2024 ingest, 17 fold)で逆転 → cand win LogLoss 0.23522→0.23466・Brier/AUC(0.7335→0.7386)改善・16/17 fold 勝ち。当初 adopted=False の唯一要因は最悪 fold(2022) の dECE +0.00169 が worst-fold ECE ガード(1e-3)超過。bad-fold 診断で「高確率域(p≥0.2, Kelly でステークが乗る本命)はむしろ候補の方が校正良好、悪化は中間ビン(0.1–0.3)の良性アーティファクト」と判明 → fold ガードを「平均 ECE tol(1e-3, 厳格)」と「worst-fold ECE tol(2e-3, 緩め=単一 fold の微小 blip で否決しない)」に分離し adopted=True。**ただし市場 diagnostic(829,885 頭): モデル p LogLoss 0.234 は市場 q 0.202 に依然負け(gap +0.032)、p−q edge bucket は新特徴で不変(モデルが「市場より過小評価」と判定した 45 万頭の実現勝率 2.2% ≪ 市場が買う馬 19.6%=edge は実現勝率と逆相関)→ 採用は予測絶対品質の改善であって「市場超過」ではない**(020 spec の「絶対校正改善≠市場超過」を実データで確認、次のレバーは公開情報の追加でなく sectional/pace 等の新シグナル)。LightGBM/binary・009 win→joint(IV)・Unknown 維持。ranking/monotonic/model family 変更/multi-output/pedigree 見直し/sectional/特徴量ストア deferred.
Decision-support display (021, in `api/` + `front/` + `eval/`; no schema change; depends 014/015 read-only): 製品目的を「市場超過の自動収益」でなく「人間が予測・確率・期待値とその信頼性を見て自分で判断する正直な意思決定支援」と明確化（[[product-goal-decision-support]]）し、表示を強化。US1 モデル p と市場 q（=(1/odds)/Σ(1/odds) vote-share）を**同一 canonical field**で併記（`predictions` に `market_win_prob`/`market_prob_source`/`canonical_consistent`/`odds_as_of`/`odds_source` 追加、`selection.market_win_probs` が p と同じ母集団で q を再正規化、母集団不一致は `canonical_consistent=false` で乖離表示抑制）— codex R1/R3: q は pseudo（FL bias 含む・p≠q）で買いシグナルと短絡させず「意見相違」中立提示（profit 語/損益色/edge ソート禁止）、q 欠損は null（0 補完しない）。US2 校正可視化: walk-forward **OOS** reliability bins（pred_mean/realized_rate/Wilson CI/件数/suppressed）を `harness.reliability_bins` で算出し `EvalResult.to_summary().eval.reliability` 経由で既存 `model_versions.metrics_summary`(JSONB) に永続 → 新 `GET /models/{mv}/calibration` が read のみ（再計算しない＝in-sample 楽観回避 codex R2、未収録は 404 typed `calibration_unavailable`）。US3 は検証先行: T016(walk-forward OOS)で馬 prior-start 数の校正差を検証 → weak−strong ECE +0.00211(閾値 +0.002 を +0.00011 のみ超過)と薄く、codex 確認で「校正信頼度シグナルとしての採用は NG(bootstrap CI/fold 単調性 未検証)」→ **中立な出走歴の事実 `prior_starts_band`(few/some/many、weak/strong 語・損益色・ソートなし、『的中確信ではない』明示)としてのみ採用、校正信頼度の解釈は deferred**。憲法: II 表示派生値(q/reliability/data_backing)をモデル特徴に流用しない(leak-guard test)・p≠q 分離、III reliability は eval OOS 由来・独自指標を作らない、IV p,q 同一 canonical field、V pseudo は単一 PseudoBadge ラベル・audit/odds source 表示、VI API 契約先行(committed `front/openapi.json`+drift-check)・read-only 厳守(全 path GET のみ test)。EV/控除率表示・生q vs FL補正q'併記・新シグナルは deferred(analyze G1/G2)。バックエンド(api/eval/training 契約+テスト)実装済み、front コンポーネント(PQCompare/CalibrationChart/DataBackingBadge)と US3 は継続。
Pace/time features (023, in `features/` + `eval/` + `training/`; no schema change, FEATURE_VERSION features-005→006): 020 の後継レバー。既に ingest 済みの result-time データ(`race_results.last_3f`/`finish_time`/`finish_time_diff`/`corner_orders`、`race_horses.running_style`)を**対象レースより前の馬実績だけ** as-of 集計してリーク安全な特徴に。`features/pace_features.py`: 各過去レースで **in-race relative**(そのレースの finisher 平均との差＝距離/馬場/going を自然に吸収)を作り→recent-N rolling→`merge_asof(allow_exact_matches=False)`。pace_time group(rel_last3f_avg/best・rel_time_avg・finish_diff_avg/best) + position_style group(任意・ablation-gated: rel_corner_pos_avg・front_runner_rate・closer_rate)。loader 拡張(従来 last_3f のみ→上記追加)が最大のリーク危険点 → **拡張 leak-guard**(今走結果・**同走馬の今走値**・同日他レース・**未来年の時計基準**を変えても不変、テスト済)。`registry.FEATURE_GROUPS`+`FEATURE_VERSION`、builder 結線、ALL_COLUMNS 自動導出。採用ゲート(020 流用+codex P0 強化): `feature_eval` に **strict majority**(`n_win*2>n_folds`、偶数 fold の半数通過を是正)+ **worst-fold LogLoss 上限**を追加、`training feature-eval --drop-groups`(既定=023 群)で baseline=features-005。**実データ(2007–2024,17 fold): cand win LogLoss 0.23466→0.23333・AUC 0.73856→0.74469(+0.006)・Brier↓・17/17 fold 勝ち。research feature-eval gate は worst-fold dECE +0.00226>2e-3 で adopted=False**(020 同型＝識別力↑だが 1 fold の校正悪化＝discrimination↔calibration トレードオフ)。**決定: 識別力ゲインが大きいため features-006 を operational 採用(serving モデル再学習 lgbm-023)し、ECE 悪化＝過信は 017 のモデル p 校正(power γ<1、Kelly/recommendation 時に opt-in `--p-gamma`/fit、betting 層で適用＝win-eval とは別経路)で相殺する設計**(017 は overconfidence×Kelly 増幅をまさに補正)。market_edge は SECONDARY(市場超過は努力目標、時計/上がりは市場織り込み済みの公算 codex P1)。LightGBM/binary・009 win→joint(IV)・Unknown 維持。furlong 毎 sectional/本格スピード指数/ranking/monotonic/multi-output deferred、次候補=条件替わり・バイアス逆行(市場が落としやすい相互作用)。
Feature materialization (025, in `features/`; no schema change, FEATURE_VERSION 不変=features-006): 重い as-of/過去由来特徴(history・020 recent_form/aptitude/class_transition/human_form・023 pace/position_style)を「生成フェーズ」に分離し parquet + manifest に materialize、builder は read+merge する infra(新シグナル無し、026 血統適性が載る土台)。**単一実装**(codex P0): `materialize.build_asof_features` が唯一の as-of 源＝in-memory builder・serving fallback と同じブロック関数を呼ぶ(二重実装→ドリフト無し)。**bit パリティ**(最重要・非交渉): materialize 経路 == in-memory の `build_feature_matrix` 出力が `assert_frame_equal(check_exact=True, check_dtype=True)`(実 DB 920k 行で確認)＝FEATURE_VERSION 据え置き＝採用済みモデル予測不変(憲法 III/V)。**fail-closed staleness**(codex P0): manifest に **source_fingerprint**(races/race_horses/race_results 射影列の決定論ハッシュ、object 列=corner_orders 等は str 化)を保持、read 時に `data_through` まで restrict して再計算し不一致なら例外(範囲内 backfill も検知＝黙って古い値を出さない)。`materialized_columns()` を registry から機械導出(STATIC_COLUMNS=現レース行のみで決まる 16 列 venue/distance/.../field_size を除外、odds/payout/dividend トークン 0=leak-guard)。read 経路は opt-in(`use_materialized=True`、既定 off＝現行経路がパリティ基準)、`use_materialized` 時はフルプール load(fingerprint が materialize 範囲全体をカバー)＋end_date は最終行フィルタのみ。serving 未来レース(parquet 非カバー)は `has_future_rows`→単一レース fallback 計算(生成と同一実装)。リーク境界不変(per-row as-of は pool-end 非依存＝materialize 安全、憲法 II)。parquet は artifacts 配下・非コミット・DB から決定論再生成(憲法 V)。`features materialize` CLI(DB read-only)。スキーマ変更なし(migration head=0006 不変・`__tablename__` 追加なし)。feature_snapshots テーブル化/自動再生成/血統特徴(026)/static 列 materialize/partitioning deferred.
Pedigree-aptitude features (026, in `features/` + `training/` + `eval/`; no schema change, FEATURE_VERSION features-006→007): 020/023 の公開情報特徴は識別力↑だが市場 q を超えられなかった → 「市場が情報を持ちにくいデビュー馬・少数出走馬」に効く初のレバーとして血統(種牡馬)適性を追加。**実 DB 実態(plan 時確認)**: horses の血統「名前」列 sire_name/damsire_name は ~100% populate だが「ID」列 sire_id 等は ~0%(ingest は名前のみ・scrape の血統 ID 解決未稼働) → **集計キーは sire_name(名前)**、ID 版 deferred。`features/pedigree_features.py`: 「同じ種牡馬の“他の”産駒の、対象レース日より前の走力」を as-of 集計。**リークの核(自馬除外)**: sire 母集団に対象馬自身が入るため `他産駒 = sire累積(cumsum−当日) − 自馬累積` で strictly-before かつ自馬を控除(O(n)、history との二重計上を防ぐ。human_form の同日除外だけでは不足)。sire_aptitude group(win_rate/avg_finish/starts/dist_band_win_rate/surface_win_rate, 距離帯/芝ダートは 020 定義再利用・条件付きは min_starts=10 未満 NaN) + damsire_aptitude group(任意・ablation-gated, 全体 win_rate/avg_finish)。dam は産駒数極小で不採用。codex R2 採用: `_normalize_name`(NFKC+strip) で名前キーの全角半角/空白ゆれを吸収。Unknown=NaN 維持・血統列は float64 固定(プール依存 dtype ドリフト防止＝パリティ)。**025 連携**: `Frames` に optional horses、`build_asof_features` に pedigree ブロック(単一 as-of 源)、`source_fingerprint` を horses 血統列込みに拡張(血統 backfill 検知=fail-closed)。025 read 経路を改修(static は end_date 窓で計算＝age 等の int→float プール依存ドリフト解消、フルプールは fingerprint 検証専用)。リーク境界(憲法 II): strictly-before＋自馬除外＋同日除外、オッズ/今走結果は非特徴、leak-guard test(自馬過去/今走・同日他産駒・未来 の不変)。**採用ゲート(020/023 同型 walk-forward OOS, `feature-eval` 既定 drop=026 群で baseline=features-006)**: 実 DB 18 fold で cand win LogLoss 0.23340→0.23313・AUC 0.74534→0.74693・Brier↓・14/18 fold 勝ち・worst_dECE +0.00155(<2e-3)・worst_dLogLoss +0.00032(<5e-3) → **primary_pass=True・ADOPTED=True**(初回 OOS で素直に採用された初の特徴)。実 DB カバレッジ: sire_win_rate 99.4%、デビュー馬(出走の10.5%)の98.6%に sire 特徴(他産駒由来)。market_edge は SECONDARY(市場超過は採否バーでない、製品目的=意思決定支援)。LightGBM/binary・009 win→joint(IV)・Unknown 維持。ID ベース結合(scrape 血統 ID 解決後)/embedding(036 前例)/3代血統・インブリード・ニックス/dam 本格活用/距離適性連続化 deferred。
Low-cost features (030, in `features/` + `training/`; no schema change, FEATURE_VERSION features-007→008): DB に 99-100% あるのに未活用の安価リーク安全特徴を 5 group 追加。handicap(斤量 carried_weight/ratio/rel/change)・place_rate(複勝/3着内/距離帯別)・human_form_plus(騎手調教師複勝・直近・×芝ダ・コンビ・乗り替わり)・course_aptitude(venue 自馬率)・season(月/季節)。静的群=static_features、as-of 群=新 lowcost_features→025 build_asof_features。running_style は corner_orders(結果)由来=今走脚質リークで除外し §3(031)へ、draw_bias 冗長/grade スパース(26.8%)で deferred。**結果(18 fold OOS)**: per-group では place_rate のみ単独採用だったが **bundle(全5群)は LogLoss 0.23316→0.23277・AUC 0.74682→0.74810・ECE 0.00954→0.00893・16/18 fold で ADOPTED**＝個別限界寄与は群間交互作用を過小評価 → **全5群採用(lgbm-030 active 昇格, lgbm-026 retired)**。実 DB parity bit 一致(3665×74)。
Scrape: polite netkeiba fetch (robots/rate-limit/cache/UA/backoff) of entries+odds+results into existing core tables; netkeiba IDs map to JRA-VAN via id_mappings only (no guess-join) — mapped→canonical_id, unmapped→unique `nk:{id}` surrogate + UNMAPPED queue (debut/leak-safe); future race_id must be a valid JRA-VAN 12-digit or no row written (no fake IDs); results backfill is INSERT-ONLY (never overwrite JRA-VAN); pre-race odds overwrite ONLY result-pending races (protect JRA-VAN final odds); idempotent + ingestion_jobs audit; parsers tested on saved HTML fixtures (network-free). No schema change. Odds never a model feature. 2007+.
<!-- SPECKIT END -->

## Codex agent の使用方針

**Proactively use codex** — 以下のトリガーに該当したら、ユーザーが明示的に頼まなくても
default で codex 系を起動する。「念のため」ではなく該当時は並走が既定動作。

### 起動トリガー（1 つでも該当したら使う）

**ユーザー発話キーワード**（second opinion 系）:
「設計どう思う」「2 案出して」「これで合ってる？」「比較して」「レビューして」
「アーキ」「方針」「相談」「迷ってる」「どっち」「妥当？」
→ `codex:codex-rescue` agent を Agent tool で並走させ意見を取る

**作業内容**:
- 新規 spec / plan / 設計ドキュメントを書く / 更新する時 → second opinion を必ず取る
- 新規モジュール・新規サービス・新規 API endpoint の設計 → 実装案を並走生成し比較
- 複数ファイル横断のリファクタ・移行・スキーマ変更 → アプローチを並走比較
- ML パイプライン / 学習ロジック / 特徴量設計の変更 → 独立検証（過去 035/036 で
  片側判断の校正ミスあり、[[pedigree-embedding-036-result]] 参照）
- 同一バグで 1 回直して再発、または root cause が読めない → `/codex:rescue` で diagnosis

**スタック時の追加ルール**: 「もう一回試す」前に必ず codex を呼ぶ。再試行 2 回目は禁止。

### 使わない場面（狭めに定義）
- 1 ファイル内の rename / typo / import 修正
- 既に固まった方針の機械的な反映
- ユーザーが明示的に「Claude だけで」と指示

### 実行ルール
- 並走時は Agent tool を単一メッセージから並列発火（Claude 自身の作業と同時進行）
- 初回は `/codex:setup` で CLI 稼働を確認
- codex 結果は鵜呑みにせず Claude 案と突き合わせ、相違があればユーザーに提示
- 起動を見送った場合、応答の冒頭で「codex 不要と判断した理由」を 1 行宣言する
  （これが最大の効き目 — 見送り判断が可視化される）
